{
  "name": "mute-github-notifications",
  "version": "1.0.0",
  "description": "Extension for hide Github notifications",
  "author": "Vaillant Samuel <samuel.vllnt@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "manifest": "cp manifest.json dist/",
    "contentScript": "cp contentScript.js dist/",
    "prepare": "mkdir dist",
    "build": "yarn clean && yarn prepare && yarn manifest && yarn contentScript"
  }
}
